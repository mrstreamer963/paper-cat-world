import { describe, expect, it } from "vitest"; import { Camera } from "../../src/render/camera.js";
describe("Camera", () => { it("round-trips points and preserves the zoom anchor", () => { const c = new Camera(); c.x = 123; c.y = -44; c.zoom = 1.7; const p = { x: 52, y: 91 }; expect(c.screenToWorld(c.worldToScreen(p))).toEqual(expect.objectContaining({ x: expect.closeTo(p.x), y: expect.closeTo(p.y) })); const anchor = { x: 300, y: 200 }, before = c.screenToWorld(anchor); c.zoomAt(anchor, 2.4); expect(c.screenToWorld(anchor)).toEqual(expect.objectContaining({ x: expect.closeTo(before.x), y: expect.closeTo(before.y) })); }); });
